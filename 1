#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <windows.h>
#include <locale.h>

int main (){
	setlocale(LC_ALL, "Spanish");
	HANDLE hConsole = GetStdHandle(STD_OUTPUT_HANDLE);	
	printf ("		     \n        			  ");
	
	SetConsoleTextAttribute(hConsole, FOREGROUND_RED);
	printf ( "Conversión de Celsius a Fahrenhiet y viceversa\n");
	printf("\n");
	
	float F1;
	float F2;
	float C1;
	float C2;
	
	SetConsoleTextAttribute(hConsole, FOREGROUND_BLUE);
	printf("Digite los grados Fahrenheit:");
	scanf("%f",&F1);
	
	C1=(F1-32)/1.8;
	SetConsoleTextAttribute(hConsole, FOREGROUND_RED | FOREGROUND_GREEN);
	printf("Los grados Celsius son: %.2f\n",C1);
	
	SetConsoleTextAttribute(hConsole, FOREGROUND_BLUE);
	printf("\nDigite los grados Celsius:");
	scanf("%f",&C2);
	
	F2=(9/5.0)*C2+32;
	SetConsoleTextAttribute(hConsole, FOREGROUND_RED | FOREGROUND_GREEN);
	printf("Los grados Farenheit son: %.2f",F2);
	
	SetConsoleTextAttribute(hConsole, FOREGROUND_RED | FOREGROUND_GREEN | FOREGROUND_BLUE);
	
	getche (); //para la pausa
	return 0;
}
